import React from 'react';
import styles from './Blurbs.module.scss';

export default function SustainExplain() {
    return <div className={`${styles.main}`}>
        <div className={`${styles.box}`}>
            As someone who is a strong believer in sustainability, I think actions speak much louder than words! Here are a few things that I think we as individuals can do reduce our impact on the earth.
        </div>
    </div>
}